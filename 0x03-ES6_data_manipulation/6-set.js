/**
 * Creates a new Set from an array.
 *
 * @param {Array} arg - The array to create the Set from.
 * @returns {Set} - A new Set containing the elements from the array.
 */
function setFromArray(arg) {
  const result = new Set(arg);
  return result;
}
export default setFromArray;
